<!-- No outer-loop acceptance group: design.md → Test Strategy records why. This change's
     outermost surface is a library API — `src/main.rs` is untouched and no caller consumes
     a `Schema` until `changes-from-files` — so an acceptance test would drive an entry
     point that ships to nobody. The unit tier reaches every collaborator these functions
     have: a real filesystem for three files, and a real YAML parser that is deliberately
     never faked.

     No group carries a `parallel-after` marker. Groups 2 through 6 all edit
     `src/schema.rs`; the tasks instruction permits parallel dispatch only for groups
     touching different file trees with no shared mutable state, and one file is shared
     mutable state.

     All nine concentration points from `openspec/config.yaml` are accounted for.
     Scheduled: nothing spawns outside `cli` (7.2 for the source text, and 1.7 for the
     build graph, which is the half a source grep cannot see); nothing writes inside
     `openspec/` (3.4 and 6.6, both asserting directory entries, bytes, and mtimes rather
     than a file listing — `testutil::snapshot` already records directories, extended by
     `repo-resolution` task 1.4, so no extension is needed here); an absent case for every
     external dependency (2.3 for both configuration files absent, 6.2 for a schema that is
     not vendored, 5.5 for a schema with no tasks artifact, 4.7 for a document that is not
     a schema at all); coverage counts the whole crate (10.5, and 6.7 keeps the one
     composition to a line that still has a discriminating test). Not applicable,
     deliberately: no view is added, so "views perform no I/O" and "view tests at 60 and
     120 columns" have nothing to bind to; the `Change` type does not exist yet, so
     `from_files`/`from_cli` agreement is not at stake; no attribution path exists, so
     "attribution must not guess" has nothing to test; no agent is named, so the 32-character
     Herdr name cap does not apply.

     Contract gate: this change alters no plugin manifest key and no `config.toml` key, so
     that gate does not fire. It does change `Cargo.toml`, which the live `plugin-build`
     requirement pins, so 1.2 and 1.8 are the gate for that instead — one before the edit
     and one after. It also consumes no published contract of another module: the
     repository root arrives as an argument.

     Persistence gate: none applies, and design.md → Persistence and Rollout says so per
     item. Nothing is stored, migrated, indexed, or cached — 6.7 is the gate that records
     why `select` and `load` are split rather than cached, and confirms no `static` and no
     `OnceLock` was added.

     None of the command checks in this file may be committed as a Rust test. A test that
     reads `Cargo.toml` and asserts `yaml-rust2` is listed proves only that the string was
     typed twice; the command that consumes the manifest is the check. -->

## 1. The YAML dependency and the module skeleton
<!-- kind: operational -->

Covers spec scenarios "The declared dependency set is exactly the argued crates", "Every
package in the normal build graph declares an MSRV no higher than the crate's", and "The
resolved build graph is small and proc-macro-free".

- [ ] 1.1 CHECK: Confirm the starting state, so any later failure belongs to this change — `make check` is green at HEAD and reports the current line coverage (record the number; planning-time baseline is 97.51% over 1806 lines, itself to be re-confirmed rather than trusted), `git status --short` shows no unrelated uncommitted work, and `cargo tree -e normal --prefix none` names exactly `herdr-openspec`, `toml`, `serde_core`, `serde_spanned`, `toml_datetime`, `toml_parser`, `toml_writer`, and `winnow`. Record that list: 1.7 asserts what it becomes
- [ ] 1.2 CHECK: Contract gate, **before** editing `Cargo.toml` — re-read the live requirement `openspec/specs/plugin-build/spec.md` → "The crate produces one binary from a declared dependency set" as it stands at implementation time, and confirm this change's delta spec still replaces exactly what is there. The delta is written as REMOVED + ADDED rather than MODIFIED, for the reason proposal.md records; confirm the REMOVED block still names the requirement by its exact live heading, or `openspec validate --strict` rejects it
- [ ] 1.3 CHANGE: Confirm the current stable `yaml-rust2` version at implementation time — `cargo search yaml-rust2`, or the crates.io API — rather than trusting design.md's `0.12.0`, which was current on 2026-09-04. `AGENTS.md` → Conventions requires this. If a newer version has been published, use it and **re-run 1.7 and 1.8**, since both pin resolved values that a version bump can move
- [ ] 1.4 CHANGE: Add to `Cargo.toml` `[dependencies]`: `yaml-rust2 = { version = "<version>", default-features = false }`. `default-features = false` is load-bearing rather than stylistic — the default `encoding` feature pulls `encoding_rs`, which 1.7 asserts is absent — and it matches the shape `toml` is already declared with. Declare no feature list: the crate needs none, and an empty list is the explicit statement `plugin-build` asks for
- [ ] 1.5 CHANGE: Add an empty `src/schema.rs` and declare `pub mod schema;` in `src/lib.rs`, leaving `config`, `resolve`, `state`, and the invocation-parsing code untouched. `testutil` needs no new helper: `ScratchDir`, `write_with_mode`, `symlink`, `canonical`, and `snapshot` all exist and this change extends none of them — if a fixture turns out to need one, add it here rather than inside a test module, and say so in 1.9
- [ ] 1.6 VERIFY: `cargo build --locked` **fails** first — the committed lock predates the new dependency — then `cargo build` succeeds and `Cargo.lock` moves. Confirm `git diff --stat -- Cargo.lock` shows additions only, and that `git diff -- Cargo.lock` adds exactly the five packages `yaml-rust2`, `arraydeque`, `hashlink`, `hashbrown`, and `foldhash` plus their checksums, with no unrelated version bump swept in. Then commit the lock and confirm `cargo build --locked` succeeds. Asserting the failure first is what proves `--locked` is doing anything: it exits 0 for any lock that already matches
- [ ] 1.7 VERIFY: The resolved normal build graph. Run the `GRAPH` block from design.md → Test Strategy and confirm it matches; then separately confirm `cargo tree -e normal` names no `syn`, `quote`, `proc-macro2`, `serde_derive`, or `encoding_rs`. Keep the second check even though the exact-list equality implies it — a later relaxation of the list to a subset would otherwise stop catching a proc macro. Do **not** count `Cargo.lock` entries: the lock holds optional resolutions cargo never builds
- [ ] 1.8 VERIFY: The declared set and the MSRV floor. Define `$DEPS` in the shell from design.md → Test Strategy (it is a shorthand, not an exported variable) and run `cargo metadata --no-deps --format-version 1 | python3 -c "$DEPS"`; then run `python3 -c "$MSRV"`. Use the `sys.exit`-on-mismatch forms the design carries, never a bare `assert` — `python3 -O` or an inherited `PYTHONOPTIMIZE` strips `assert` and the check silently exits 0 for any dependency list (verified). `$MSRV` must print its count line: a silent exit 0 from it would mean it checked nothing, which its two vacuity guards exist to prevent
- [ ] 1.9 VERIFY: `cargo test --all-features` is still green with the new empty module declared and the new dependency present but unused — the skeleton compiles, nothing regressed, and `cargo clippy --all-targets --all-features -- -D warnings` does not object to an unused dependency (it does not; `cargo udeps` would, and is not a gate here)

## 2. Schema name selection
<!-- kind: behavior -->

This group is entirely pure: `schema_key`, `is_legal_name`, and `declared_name` take `&str`
and `FileText` values and touch no filesystem. Group 3 adds the edge that produces those
values. Splitting them this way is what lets these eleven scenarios run without a scratch
tree, and it is the same move `resolve::path_candidates` made for the `PATH` string.

- [ ] 2.1 RED: Write failing unit tests in `src/schema.rs`, one per spec scenario, named after them: "A change's own declaration wins over the project's", "The project declaration answers when the change declares nothing", "Nothing declared anywhere yields the default", "A blank declaration is not a declaration", "A declaration of the wrong type is not a declaration", "A file that is not valid YAML falls through and is named", "An empty or comment-only document declares nothing without complaint", "Only the first YAML document is consulted", "A name containing a path separator is rejected and falls through", "Every rejected shape is rejected, and a legal name with a dot is not", "An illegal project name still falls through to the default". Build every input as a `&str` literal and a `FileText` value; no test in this group creates a file
- [ ] 2.2 RED: Make the precedence test discriminating rather than merely green-able. "A change's own declaration wins over the project's" must **also** assert, in the same test, that dropping the change source yields the project's name — otherwise an implementation that ignores the project file entirely passes, and so does one that ignores the change file when the two happen to agree. Assert the whole `Selection` value with `assert_eq!`, including `source` and `problems`, not just the name
- [ ] 2.3 RED: "Nothing declared anywhere yields the default" must assert `problems.is_empty()`, not only the name. This is the absent-dependency case for both configuration files at once, and the empty-problems clause is the only thing that rejects an implementation recording a fallback for a file that never existed — which would put a spurious message in the header for every repository that simply uses the default
- [ ] 2.4 RED: "A declaration of the wrong type is not a declaration" asserts four values in one test — a nested mapping, a sequence, `null`, and a document that is a bare top-level scalar rather than a mapping. An implementation that string-formats any YAML node returns `{...}` or `[...]` as a schema name and fails all four; one that only handles the mapping case fails the rest. "A blank declaration is not a declaration" asserts both `"   "` and `""` in one test, reusing the `config::non_blank` rule rather than restating it
- [ ] 2.5 RED: "An empty or comment-only document declares nothing without complaint" asserts `problems.is_empty()` for both inputs. `yaml-rust2` returns **zero documents** for each (verified), so an implementation indexing `[0]` panics here rather than failing an assertion — check the test reports a panic and not a compile error, so the RED is the behaviour and not the harness
- [ ] 2.6 RED: "Every rejected shape is rejected, and a legal name with a dot is not" asserts six values in one test — `..`, `.`, `a/b`, `/abs`, a value containing a NUL byte, and `v1.2-tdd`. The sixth is load-bearing: without it, an `is_legal_name` that returns `false` unconditionally passes every other case in the group, since they all expect the fall-through. "A file that is not valid YAML falls through and is named" asserts that the problem text contains the parser's own message, not a generic one, so a user can find the offending line
- [ ] 2.7 Confirm every failure is the missing function rather than a broken test module — check that `cargo test --all-features schema::` **names** the new tests rather than reporting "0 passed … N filtered out", which a cargo test-name filter does for any nonsense filter and returns 0
- [ ] 2.8 GREEN: Implement `schema_key(text: &str) -> Result<Option<String>, String>`. `Ok(None)` when the text holds no document; `Err` when it is not valid YAML, when its first document is not a mapping, or when `schema:` is present with a value that is not a string — including `null`. `Ok(Some(trimmed))` for a non-blank string, applying `config::non_blank`'s rule. Only the first document is read. Every parser error becomes an `Err` carrying the parser's message; nothing unwraps and nothing indexes a document list without checking it is non-empty
- [ ] 2.9 GREEN: Implement `is_legal_name(name: &str) -> bool` — non-blank after trimming, no `/` or `\`, not `.` or `..`, not absolute, no NUL byte. Then `declared_name(change, project) -> Selection` walking the three sources in order, treating an `Err` from `schema_key` and an illegal name identically: record one problem naming the file and continue to the next source, never win and never stop. `FileText::Absent` contributes nothing and records nothing; `FileText::Unreadable` records one problem naming the file
- [ ] 2.10 REFACTOR: Extract the "read one YAML document from text" step if `parse` in group 4 will share it — both need "first document or nothing, never a panic". If the two turn out to want different shapes, say so here rather than forcing a shared helper
- [ ] 2.11 Run the group tests — `cargo test --all-features` green, no regressions to the existing `config`, `resolve`, `state`, or invocation tests

## 3. The selection filesystem edge
<!-- kind: behavior -->

- [ ] 3.1 RED: Write failing unit tests named after their spec scenarios: "No change directory is supplied at all", "An unreadable file falls through and is named", "A repository tree is byte-identical after selection", "A missing configuration file is not created". Build every fixture under a `testutil::ScratchDir`; never assert against a path in the real repository
- [ ] 3.2 RED: "No change directory is supplied at all" plants a `.openspec.yaml` **beside the repository root** — not inside it — and asserts the resolved name comes from the project. An implementation that derives a change directory from the repository root rather than honouring the `None` argument reads that file and fails
- [ ] 3.3 RED: "An unreadable file falls through and is named" creates `openspec/config.yaml` as a **directory**. `std::fs::read_to_string` returns an I/O error rather than `NotFound` for it, which is what separates `FileText::Unreadable` from `FileText::Absent`; an implementation mapping every `Err` to `Absent` records no problem and fails the count assertion
- [ ] 3.4 RED: Write the two containment tests in the same pass — "A repository tree is byte-identical after selection" (snapshot paths **including directories**, bytes, and modification times through `testutil::snapshot`, run `select` twice, assert snapshot equality) and "A missing configuration file is not created" (assert neither the file nor the absent change directory exists afterwards). Read modification times through `std::fs::Metadata`, never by shelling out to `stat`, whose flags differ between BSD and GNU. Assert bytes and mtimes, not only the listing: a rewrite of identical length passes a listing-only check. Be honest about what these two are — **guards, not behaviour drivers**: before `select` exists they fail to compile, which is not the same as failing for the behaviour; what they genuinely fail against is an implementation that creates a file or directory while reading
- [ ] 3.5 Confirm the failures are the missing edge and not a broken fixture — assert inside at least one test that the fixture file exists and has the expected bytes before the call under test
- [ ] 3.6 GREEN: Implement `read_file(path) -> FileText`, mapping `ErrorKind::NotFound` to `Absent`, any other `Err` to `Unreadable` carrying the message, and success to `Read`. Then `select(repo, change_dir) -> Selection`, reading `<change_dir>/.openspec.yaml` only when `change_dir` is `Some`, and `<repo>/openspec/config.yaml` always, and handing both to `declared_name`. Treat every filesystem `Err` as a value, never a panic — no `unwrap`, no `expect`
- [ ] 3.7 REFACTOR: Nothing in `select` should do more than read two paths and delegate. If a rule leaked into it from group 2, move it back; if none did, say so
- [ ] 3.8 Run the group tests — `cargo test --all-features` green, no regressions

## 4. Parsing a schema document into artifacts
<!-- kind: behavior -->

Entirely pure: every scenario here is `parse(name, text)` over a `&str` literal. Group 6
maps its `Err` onto `LoadError::Invalid` and adds the filesystem.

- [ ] 4.1 RED: Write failing unit tests named after their spec scenarios: "Artifact order is the file's order, not alphabetical", "Only the first YAML document of the schema file is used", "Entries missing `id` or `generates` are skipped and the rest survive", "A blank `id` or `generates` is skipped like an absent one", "An entry that is not a mapping is skipped", "A `generates` that escapes the change directory is skipped", "Unused keys being absent or malformed does not make an entry unusable", "A schema whose every entry is unusable is invalid rather than empty", "An empty, comment-only, or non-mapping document is invalid", "An absent, non-sequence, or empty `artifacts:` key is invalid", "Flow style, anchors, and CRLF all parse", "A schema file whose block scalars mimic structure still parses correctly"
- [ ] 4.2 RED: "Artifact order is the file's order, not alphabetical" uses **three** artifacts named `zeta`, `alpha`, `middle` in that order. Two artifacts distinguish a sorted implementation from a correct one only by luck of naming, and a reversed implementation not at all; three named this way fail both
- [ ] 4.3 RED: "A `generates` that escapes the change directory is skipped" asserts the rejected values `../outside.md`, `/etc/passwd`, and `a/../../b.md` **and**, in the same test, the accepted values `specs/**/*.md` and `sub/dir/file.md`. Without the accepted pair, a predicate rejecting every value containing `/` or `.` passes — and `specs/**/*.md` is the real vendored schema's own value, so rejecting it would break the tab bar on this very repository
- [ ] 4.4 RED: "Unused keys being absent or malformed does not make an entry unusable" asserts `problems.is_empty()`. This is the scenario that rejects an implementation validating the CLI's whole artifact schema — `description` and `template` are required by the CLI's Zod definition and by nothing here, and failing on a field the dashboard never renders would fail closed on a schema the plugin can render perfectly
- [ ] 4.5 RED: "Flow style, anchors, and CRLF all parse" is the scenario that distinguishes a YAML parser from a line scanner, and it is the one that fails if the dependency is ever replaced with a hand-rolled subset. Write all three inputs in one test: a flow-style `artifacts: [{...}, {...}]` one-liner asserting two ordered artifacts and the tasks artifact; an anchored mapping referenced by alias inside `artifacts:` asserting the aliased artifact loads; and a schema whose lines end `\r\n` asserting a result equal to the same schema with `\n`. Say in a comment why the test exists, so it is not deleted as redundant
- [ ] 4.6 RED: "A schema file whose block scalars mimic structure still parses correctly" builds an `apply.instruction: |` block containing `- id: fake`, `generates: fake.md`, and `tracks: fake.md` **at the same column an artifact's keys use**, and asserts exactly the two real artifacts with no `fake` entry and the real `tracks` target. Model the indentation on the vendored `openspec/schemas/tdd/schema.yaml`, whose `apply.instruction` content genuinely sits at four spaces — the same column as `    generates:` under an artifact. This is the concrete failure a hand-rolled scanner has, written down as a test
- [ ] 4.7 RED: "An empty, comment-only, or non-mapping document is invalid" asserts four inputs in one test — empty, comment-only, a bare scalar `just text`, and a top-level sequence. The first two return **zero documents** from `yaml-rust2`, so an implementation indexing `[0]` panics rather than returning `Err`. "An absent, non-sequence, or empty `artifacts:` key is invalid" asserts three inputs, with `artifacts: []` explicit: a schema with zero tabs is not a state the detail view can render, and an implementation returning `Ok` with an empty list here is the one this case exists to reject
- [ ] 4.8 Confirm the failures are the missing `parse` and not a fixture problem — check the filter names the new tests, and assert in at least one test that the fixture text is what the test intended by matching a substring of it before parsing
- [ ] 4.9 GREEN: Implement `parse(name: &str, text: &str) -> Result<ParsedSchema, String>`. Load the first document or return `Err`; require it to be a mapping; require `artifacts:` to be a non-empty sequence. For each entry, in order: require a mapping with a non-blank string `id` and a non-blank string `generates` that is relative, contains no `..` segment, and contains no NUL; skip anything else with one problem naming its **zero-based position**. Read no other key except the top-level `name:`, compared against the `name` argument and recorded as a problem when they differ. If skipping empties the list, return `Err`. Every parser error becomes the `Err` string verbatim, so the caller can put the parser's own message in front of a user
- [ ] 4.10 REFACTOR: If the entry-level checks read as a wall of `if let`, extract one `artifact_from(node, position) -> Result<Artifact, String>`. Keep the position out of the artifact type — it exists only for the problem text
- [ ] 4.11 Run the group tests — `cargo test --all-features` green, no regressions

## 5. The tasks artifact
<!-- kind: behavior -->

- [ ] 5.1 RED: Write failing unit tests named after their spec scenarios: "`apply.tracks` selects an artifact whose id is not `tasks`", "An absent `apply` block falls back to the artifact with id `tasks`", "An `apply` block without `tracks`, and an explicit `tracks: null`, both fall back to the id", "A `tracks` value matching nothing yields no tasks artifact even when an id `tasks` exists", "A schema with neither a `tracks` match nor an id `tasks` loads without one", "`tracks` matches on `generates`, not on a filename suffix", "A `tracks` value of the wrong type falls back to the id"
- [ ] 5.2 RED: The headline scenario must use a fixture where the two rules **disagree**: artifacts `checklist` (generates `tasks.md`) and `tasks` (generates `notes.md`), with `apply.tracks: tasks.md`. Assert the tasks artifact is `checklist` **and** is not `tasks`. On the vendored `tdd` schema both rules select `tasks`, so a fixture modelled on it is green against the id-first implementation — which is exactly what `SPEC.md`'s `role: tasks` wording would have produced. Assert the whole `Artifact` value, not just its id
- [ ] 5.3 RED: "A `tracks` value matching nothing yields no tasks artifact even when an id `tasks` exists" is the subtlest rule in the change. The fixture holds an artifact with id `tasks`; `apply.tracks` names a `generates` no artifact has. Assert `tasks.is_none()` and one problem naming the missing value. An implementation that falls back to the id whenever the `tracks` lookup misses returns `Some` and fails — and that fallback is exactly what the CLI's `findTrackedTasksArtifact` does **not** do, which is why this behaviour is mirrored rather than chosen
- [ ] 5.4 RED: "An `apply` block without `tracks`, and an explicit `tracks: null`, both fall back to the id" asserts both fixtures in one test. They differ in the parse tree — a missing key is `BadValue`, an explicit null is `Null` — and an implementation branching on key presence rather than on "is null or absent" behaves differently for them. The CLI's own `tracks != null` test treats them identically even though its comment describes null as "no tracking"; mirror the code, and say so in a comment
- [ ] 5.5 RED: "`tracks` matches on `generates`, not on a filename suffix" uses artifacts `a` (generates `sub/tasks.md`) and `b` (generates `tasks.md`) with `apply.tracks: tasks.md`, and asserts `b`. An implementation comparing only the final path component returns `a`, which is first in the list — so the ordering of the fixture is part of the test. "A schema with neither a `tracks` match nor an id `tasks` loads without one" asserts the schema still **loads** with both artifacts and one problem; an implementation returning `Err` here fails closed on a schema the tab bar can render
- [ ] 5.6 Confirm the failures are the missing rule rather than group 4 regressing — run the whole `schema::` filter and check group 4's tests are still green while group 5's are red
- [ ] 5.7 GREEN: Implement the tasks-artifact rule inside `parse`. Read the top-level `apply:` mapping when present. When `apply.tracks` is a non-null string, select the artifact whose `generates` equals it exactly and stop — no id fallback on a miss. When `apply` is absent, or `tracks` is absent, null, or not a string, select the artifact whose `id` is `tasks`. Record one problem when `tracks` was a non-null string that matched nothing, one when `tracks` was present with a wrong type, and one when neither rule found an artifact. Store the result as a cloned `Artifact`, not as an index — design.md → Decisions records why
- [ ] 5.8 REFACTOR: The rule is small enough to read as one function; if it did not come out that way, extract `tasks_artifact(doc, &artifacts) -> (Option<Artifact>, Vec<String>)` so its seven scenarios sit against one signature
- [ ] 5.9 Run the group tests — `cargo test --all-features` green, no regressions

## 6. Loading from disk, the three reasons, and the composition
<!-- kind: behavior -->

- [ ] 6.1 RED: Write failing unit tests named after their spec scenarios: "The repository's own vendored `tdd` schema loads", "A schema that is not vendored is reported as such and not as broken", "A schema directory with no `schema.yaml` is not vendored", "A `schema.yaml` that cannot be read is reported as unreadable", "The directory name and the file's `name:` key disagreeing is recorded", "Invalid YAML is reported with the parser's own message", "A repository tree is byte-identical after loading"
- [ ] 6.2 RED: The three `LoadError` variants must be asserted **by variant**, never by matching text in a message — `changes-from-cli` branches on `NotVendored` and a consumer matching substrings breaks the first time a message is reworded. "A schema that is not vendored" and "A schema directory with no `schema.yaml`" both expect `NotVendored`; "A `schema.yaml` that cannot be read" creates the path as a **directory** and expects `Unreadable`. That last pair is what a `Path::exists()`-based implementation conflates, and it is the reason the variants exist
- [ ] 6.3 RED: "The repository's own vendored `tdd` schema loads" reads the real graft-vendored file through `Path::new(env!("CARGO_MANIFEST_DIR"))` — **never** the process working directory, which a test runner may change. Assert only stable properties: it parses, the artifact list is non-empty, the first artifact's id is `proposal`, an artifact with id `tasks` generates `tasks.md`, an artifact with id `specs` generates the glob `specs/**/*.md` verbatim, and no problem is recorded. Do not assert the exact list or its length: the file is vendored from `optioni/openspec-schemas` and may gain an artifact, and this change must not make an unrelated `graft sync` red. This is the one test that proves the model matches the file the plugin will actually meet — it is the test that would have caught `role: tasks` before a line was written
- [ ] 6.4 RED: "Invalid YAML is reported with the parser's own message" writes a tab-indented `schema.yaml` and asserts `LoadError::Invalid` naming the path, with the problem carrying the parser's message. Tabs are the input to use because every YAML parser rejects them for a reason a user can act on, and because it is a real error rather than a contrived one
- [ ] 6.5 RED: "The directory name and the file's `name:` key disagreeing is recorded" puts `name: original` in `openspec/schemas/renamed/schema.yaml` and asserts `Schema::name == "renamed"` — the directory that located it, which is what `openspec schema which` keys on — plus exactly one problem containing both words
- [ ] 6.6 RED: "A repository tree is byte-identical after loading" snapshots paths **including directories**, bytes, and mtimes; runs `resolve` twice; then runs it a third time naming a schema that is **not** vendored, and asserts equality after all three. The third run is the one that catches an implementation creating the schema directory on a miss
- [ ] 6.7 CHECK: Persistence gate. Nothing here is stored, migrated, indexed, or cached — confirm no `static`, no `OnceLock`, and no interior-mutable cache was added to `src/schema.rs`, and record why `select` and `load` are separate entry points rather than one cached `resolve`: `changes-from-files` iterates changes and can load once per distinct schema name, so the caller avoids the repeated 663-line parse without this module owning a cache lifetime or an invalidation rule. `repo-resolution`'s `BinCache` exists because `SPEC.md` specifies session caching for the binary probe; it specifies none for the schema
- [ ] 6.8 GREEN: Implement `load(repo, name) -> Result<ParsedSchema, LoadError>` over `read_file`: `Absent` → `NotVendored { path }`, `Unreadable(msg)` → `Unreadable { path, reason }`, `Read(text)` → `parse(name, &text)` with its `Err` becoming `Invalid { path, reason }`. Then `resolve(repo, change_dir) -> SchemaResolution`, composing `select` and `load` and folding a `LoadError` into one problem alongside the selection's problems. Keep `resolve` to the composition — if a rule appears inside it, it belongs in `select`, `load`, or `parse`
- [ ] 6.9 REFACTOR: Confirm the module's public surface is exactly what design.md → Contracts lists, and that nothing exposes a `yaml_rust2` type — a leaked `Yaml` in a signature makes the dependency permanent, which the Decisions section argues against. Check with `cargo doc --no-deps` or by reading the `pub` items
- [ ] 6.10 Run the group tests — `cargo test --all-features` green, no regressions to `config`, `resolve`, `state`, `tests/cli.rs`, or `tests/ci_workflow.rs`

## 7. Build and containment guards
<!-- kind: operational -->

Covers spec scenarios "The schema module names no process API", "Exactly one binary target
is produced at the release path", and "Each dependency is genuinely needed rather than
incidental".

- [ ] 7.1 CHECK: Re-read design.md → Test Strategy and confirm each shell block still matches what the tree looks like now — `$DEPS`, `$MSRV`, `GRAPH`, and `SPAWN` were written and run at planning time against a probe crate and against HEAD, and 1.3 may have moved the `yaml-rust2` version
- [ ] 7.2 VERIFY: Run the `SPAWN` block, both halves. The tree-wide `! grep -rnE 'process::Command|Command::new|Stdio' src/` must be clean; the module-scoped `[ -f src/schema.rs ] && ! grep -nE 'std::process|Command|Stdio' src/schema.rs` must pass. The `[ -f ... ]` guard is load-bearing: `grep` exits 2 for a missing file and `!` turns that into a pass, so a renamed or split module would silently stop being checked (verified — the module-scoped half correctly **fails** today, before `src/schema.rs` exists). Note in the task record that the tree-wide half is a task-level check `subprocess-seam` must rescope, and that only the module-scoped half is normative in `specs/schema-artifacts/spec.md`. Do **not** add a `! grep -rn '"openspec"' src/` check: it is already false at HEAD and this change necessarily adds more path components spelling `openspec`
- [ ] 7.3 VERIFY: "Exactly one binary target is produced at the release path" — `cargo metadata --no-deps --format-version 1` names exactly one `bin` target called `herdr-openspec`, and after `/bin/sh scripts/build.sh` runs, `target/release/herdr-openspec` exists and is executable. This is a live `plugin-build` scenario carried by this change's ADDED requirement; it is re-verified rather than assumed because the dependency set moved underneath it
- [ ] 7.4 VERIFY: "Each dependency is genuinely needed rather than incidental" — remove `toml` from `Cargo.toml` and confirm `cargo build` fails; restore; remove `yaml-rust2` and confirm `cargo build` fails; restore. Restore the file from `git show HEAD:Cargo.toml` written back explicitly, **never** with `git checkout -- Cargo.toml` or `git restore`, which would also discard any other uncommitted work in the tree. Then confirm `cargo build --locked` and `cargo test --all-features` are green again, and that `git status --short` shows `Cargo.toml` and `Cargo.lock` in the state this change intends rather than reverted
- [ ] 7.5 VERIFY: Re-run 1.7 and 1.8 — `GRAPH`, `$DEPS`, and `$MSRV` — now that the module actually uses the dependency, so the checks are made against the tree that ships rather than against an unused declaration
- [ ] 7.6 VERIFY: No scratch state leaked — no `herdr-openspec-test-` directory (the `ScratchDir` name shape) remains under `std::env::temp_dir()` after the suite. One that does means a test panicked and the helper's drop did not run
- [ ] 7.7 VERIFY: The graft-vendored trees are untouched — `git status --short openspec/schemas/ .claude/agents/` is empty. One unit test reads `openspec/schemas/tdd/schema.yaml`; nothing may write it, and the next `graft sync` would silently discard an edit made here

## 8. Change Review
<!-- kind: operational -->

- [ ] 8.1 CHECK: Dispatch an independent reviewer — not a fork of the implementing session — against proposal.md, all three spec files, design.md, and tasks.md plus the diff. Point it first at the concentration points that bite here: the headline tasks-artifact test using a fixture where the two rules **disagree**, and asserting the whole `Artifact`; the `tracks`-miss test proving there is no id fallback; the null-versus-absent pair asserted together; the flow-style/anchor/CRLF test genuinely failing against a line scanner; the block-scalar fixture indented to the column the vendored schema really uses; the three `LoadError` variants asserted by variant and not by message text; the `generates` traversal test carrying accepted values as well as rejected ones; `is_legal_name` having a positive case; "nothing declared anywhere" asserting empty problems; the containment tests asserting bytes and mtimes rather than listings, with the third not-vendored run; and the vendored-schema test asserting stable properties only. Give the reviewer a scratchpad path and require it to append findings as it goes rather than only in a final message — `HANDOFF.md` records two review rounds lost to `529 Overloaded`
- [ ] 8.2 CHANGE: Fix every CRITICAL, resolve or consciously accept each WARNING with a one-line reason, note SUGGESTIONs, and re-run every affected check
- [ ] 8.3 VERIFY: Confirm no blocking or unowned finding remains, and that every artifact a repair touched was updated in place rather than annotated afterwards

## 9. Documentation
<!-- kind: operational -->

- [ ] 9.1 CHECK: Re-read, at implementation time rather than from memory of this plan, the exact current wording of every section rewritten below: `SPEC.md` → Overview (the stack sentence), → Data layer → Resolution chain ("Schema"), → Degraded states (the "Schema unknown to the CLI" row), → Testing and quality gates → Unit-tested modules (the `schema::artifacts` entry); `openspec/IMPLEMENTATION-ORDER.md` → the Phase 2 `schema-model` row, the Phase 3 `changes-from-cli` row, and the Mermaid dependency graph; `AGENTS.md` → Current repo state. Record what each says now, so a correction another change already made is neither duplicated nor reverted
- [ ] 9.2 CHANGE — rewrite in `SPEC.md`: Data layer → Resolution chain → "Schema" (audience: `changes-from-files`, `changes-from-cli`, and `tasks-tab`, all planned from this paragraph). It is wrong in two ways and incomplete in two more, and this is the paragraph that made the change necessary. (a) **"The tasks artifact is the one whose entry carries `role: tasks`" is false** — no `role` key exists in the OpenSpec schema format, in the vendored `tdd` schema, in the CLI's bundled `spec-driven` schema, or in the CLI's own artifact definition. Replace it with the real rule: the artifact whose `generates` equals the schema's top-level `apply.tracks`, falling back to the artifact with id `tasks` when no `apply` block declares what it tracks — and state that a `tracks` value matching nothing yields **no** tasks artifact rather than falling back, because that is what the CLI does and the dual-source model depends on the two agreeing. (b) **"otherwise ask the CLI via `openspec schema`" names a command that does not exist** — `openspec schema` has only `which`, `validate`, `fork`, and `init`. Replace it with `openspec schema which <name> --json`, which returns `{"name","source","path","shadows"}` where `path` is the schema *directory* whose `schema.yaml` the same parser then reads, and record that its stdout is clean JSON while its "experimental" note goes to stderr — the fact a Phase 3 implementer needs and would otherwise never see. (c) Add the two sources the paragraph omits: a change's own `.openspec.yaml` overrides the project's `config.yaml`, and the default when neither declares one is `spec-driven`. (d) Add the three-tier search order — project `openspec/schemas/`, then `$XDG_DATA_HOME`, then the CLI package's built-ins — because it is *why* a CLI fallback exists at all: `spec-driven` is not vendored in any repository
- [ ] 9.3 CHANGE — rewrite in `SPEC.md`: Overview → the stack sentence (audience: every later change choosing a parser) — it names `serde_yaml`, which is published as `0.9.34+deprecated`, archived and unmaintained. Replace it with `yaml-rust2` and say in the same breath that the choice is argued in this change's design.md, so the next change needing YAML does not re-open it. Leave `serde_json` alone: `changes-from-cli` has not chosen its JSON parser yet, and pre-deciding it here would be this change making another change's decision
- [ ] 9.4 CHANGE — add to `SPEC.md`: Degraded states table (audience: `degraded-states`, which is planned from this table) — rows for the three reasons a schema does not load: **not vendored** (the artifact list is empty and the CLI tier may still supply it), **unreadable or invalid** (the artifact list is empty and the reason is named), and a schema that loads with **no tasks artifact** (every tab renders, the tasks tab is absent, and progress falls back to nothing rather than to zero). Check that the existing "Schema unknown to the CLI → per-change fall back to file mode" row does not now contradict them: that row is about the CLI *rejecting* a schema, which is a different condition from the plugin being unable to read one, and both are true at once for a schema like `outside-in-tdd`
- [ ] 9.5 CHANGE — rewrite in `SPEC.md`: Testing and quality gates → Unit-tested modules (audience: future changes choosing a tier) — the entry reads "`schema::artifacts` — `schema.yaml` to ordered tabs, including `role: tasks` detection". Correct the mechanism in place, and widen the entry to name schema *name selection* as well as artifact parsing, since the module has both and eleven of its scenarios are the former. This is a rewrite of the existing entry, not a second bullet beside it
- [ ] 9.6 CHANGE — rewrite in `openspec/IMPLEMENTATION-ORDER.md`: the Phase 2 `schema-model` row (audience: whoever reads the roadmap next, and the archive step, which is told to confirm the row still describes what was built) — it says "identify the tasks artifact by `role: tasks` rather than by the id `tasks`", which is the same false claim as 9.2(a). Replace it with the `apply.tracks` rule; add the per-change `.openspec.yaml` override and the `spec-driven` default, which the row omits and this change implements; and note that the row now carries a delta on `plugin-build`, since the change adds the crate's second dependency
- [ ] 9.7 CHANGE — rewrite in `openspec/IMPLEMENTATION-ORDER.md`: the Phase 3 `changes-from-cli` row (audience: whoever implements it) — its scope lists `openspec list --json`, `openspec status --change <n> --json`, and `openspec instructions apply --change <n> --json`. Add the schema tier it will also own: when `schema::load` reports `LoadError::NotVendored`, ask `openspec schema which <name> --json` for the schema's directory and read `schema.yaml` from there with the same parser, which covers the `$XDG_DATA_HOME` and package-built-in tiers the disk path cannot see. Carry the stdout-only fact from 9.2(b). Then **check the Mermaid graph rather than editing it reflexively**: `schema-model --> changes-from-files --> changes-from-cli` already exists, so the new obligation is transitively satisfied and a direct `schema-model --> changes-from-cli` edge would be redundant. Record the decision not to add it in the task, so a later reader does not read the absence as an oversight — this is the mirror of the edge `repo-resolution` *did* have to add
- [ ] 9.8 CHANGE — rewrite in `AGENTS.md`: Current repo state (audience: a fresh session's first paragraph) — two sentences become false. "the crate builds with its first third-party dependency (`toml`)" is now two dependencies, and "`schema-model` is next" is now done. Replace with what is true after this change: the crate reads the repository's schema and produces the ordered artifact list, and **`task-parsing` is next** — not `changes-from-files`, which also depends on `task-parsing`. Check the Phase 2 table before writing the name rather than assuming roadmap order equals dependency order. In the same pass, check whether the Important-files list should name `src/schema.rs`; it names no other source file individually, so the answer is probably no — record which way you went
- [ ] 9.9 CHANGE: Make, or record the decision not to make, one `AGENTS.md` → Conventions edit. The existing rule "check the current stable version before adding one" already covers what this change did. The only candidate new rule is "a dependency is argued in design.md and lands as a `plugin-build` delta", which is already a live spec requirement rather than a convention. `AGENTS.md` is a fixed attention budget; prefer not adding
- [ ] 9.10 VERIFY: Net size — this group rewrites six entries in place across three documents and adds roughly fifteen lines. Confirm no entry it touched now says something false: that a `role` key identifies the tasks artifact, that `openspec schema` dumps a schema, that `serde_yaml` is the YAML crate, that the crate has one dependency, or that `schema-model` is next

## 10. Lint & Verify
<!-- kind: operational -->

- [ ] 10.1 CHECK: Inspect the intended verification commands and affected tiers — the unit tier (`cargo test --all-features schema::` for one group at a time, plus the whole suite for regressions; a cargo test-name filter matching nothing exits 0, so the filtered form is a convenience and 10.4 is the gate), the command checks in groups 1 and 7, and coverage, which is affected: this change adds a module larger than `src/config.rs`
- [ ] 10.2 VERIFY: `cargo fmt --all -- --check` — clean
- [ ] 10.3 VERIFY: `cargo clippy --all-targets --all-features -- -D warnings` — 0 errors. Probe with `cargo clippy --version`, not `command -v cargo-clippy`: rustup installs that shim unconditionally, so the shim resolves even when the component is absent
- [ ] 10.4 VERIFY: `cargo test --all-features` — green, including the unchanged `config`, `resolve`, and `state` unit tests and the `tests/cli.rs` and `tests/ci_workflow.rs` integration tests, none of which this change touches. Rust has no separate type-check step; `cargo test` and `cargo clippy` cover it
- [ ] 10.5 VERIFY: `cargo llvm-cov --fail-under-lines 80` — at or above the floor, and record the resulting percentage against the 1.1 baseline. If it falls short, add tests; never lower the threshold, never add an exclusion flag, and never mark a function `#[cfg(not(test))]` to hide it from the count
- [ ] 10.6 VERIFY: `make check` — the single composite gate, exit 0. If it fails, name the failing sub-command rather than reporting a summary
- [ ] 10.7 VERIFY: `openspec validate schema-model --strict` reports the change valid, with nvm ahead on `PATH` — `export PATH="$HOME/.nvm/versions/node/v24.20.0/bin:$PATH"` — since `openspec` is not on the `PATH` a non-login shell inherits here
