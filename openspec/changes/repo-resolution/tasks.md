<!-- No outer-loop acceptance group: design.md → Test Strategy records why. The change's
     outermost surface is a library API — `src/main.rs` is untouched and no caller
     consumes `RepoSearch` or `BinResolution` until `changes-from-files` — so an
     acceptance test would drive an entry point that ships to nobody. The unit tier
     reaches every collaborator these functions have: a real filesystem and an
     environment that is a closure. The one it cannot reach, step 4's process spawn, is
     handled by deferring it to an injected hook rather than by a fake pretending to be
     `npm`.

     No group carries a `parallel-after` marker. Groups 2 through 6 all edit
     `src/resolve.rs`; the tasks instruction permits parallel dispatch only for groups
     touching different file trees with no shared mutable state, and one file is shared
     mutable state.

     All nine concentration points from `openspec/config.yaml` are accounted for.
     Scheduled: nothing spawns outside `cli` (7.2, 7.3, 7.4, and the closure-shaped
     step-4 collaborator in 5.4 and 5.8); nothing writes inside `openspec/` (2.4 and 5.5,
     both asserting bytes and mtimes, not just listings); an absent case for every
     external dependency (2.1 for a missing start directory, 3.1 for every unusable
     candidate shape, 4.1 for an empty nvm tree, 5.1 and 5.3 for all four steps empty);
     coverage counts the whole crate (6.3 keeps the one real-environment composition to a
     line that still has a discriminating test, and 10.5 holds the floor). Not applicable, deliberately: no
     view
     is added, so "views perform no I/O" and "view tests at 60 and 120 columns" have
     nothing to bind to; the `Change` type does not exist yet, so `from_files`/`from_cli`
     agreement is not at stake; no attribution path exists, so "attribution must not
     guess" belongs to `agent-attribution`; and no agent is named here, so the
     32-character cap is `plugin-state`'s, already landed.

     Contract gate: this change alters no plugin manifest key and no `config.toml` key,
     so the manifest/config gate does not fire. It does consume a published contract —
     `Config::openspec_bin` — and 5.9 re-reads `SPEC.md` → Resolution chain against what
     the chain actually implements.

     Persistence gate: none applies, and design.md → Persistence and Rollout says so per
     item. Nothing is stored, migrated, indexed, or cached across processes; the one
     cache lives and dies with the process and is never invalidated. 6.1 is the check
     that pins that behaviour rather than a migration step.

     None of the command checks in this file may be committed as a Rust test. A test that
     reads `Cargo.toml` and asserts `toml` is listed proves only that the string was
     typed twice. -->

## 1. Module skeleton and fixture builders
<!-- kind: operational -->

- [ ] 1.1 CHECK: Confirm the starting state, so any later failure belongs to this change — `make check` is green at HEAD and reports the current line coverage (record the number; 97.65% over 895 lines at the time this was planned), `cargo metadata --no-deps --format-version 1 | python3 -c "$DEPS"` passes with the normal-kind dependency list exactly `["toml"]`, and `git status --short` shows no unrelated uncommitted work
- [ ] 1.2 CHANGE: Add an empty `src/resolve.rs` and declare `pub mod resolve;` in `src/lib.rs`, leaving `config`, `state`, and the invocation-parsing code untouched
- [ ] 1.3 CHANGE: Extend `src/lib.rs` → `testutil` with the fixture builders the tests below need, beside the existing `ScratchDir` and `snapshot`, which are reused **unchanged**: a builder that writes a file and sets an explicit mode (`std::os::unix::fs::PermissionsExt`), and a symlink helper (`std::os::unix::fs::symlink`). Set the mode explicitly in every case — never rely on the process umask, which differs between a shell and a CI runner and would make "mode 0644" mean something else. The fixture "binary" is a short text file, never a copied real executable
- [ ] 1.4 CHANGE: Add a `testutil` helper that canonicalizes an expected path, because every discovery assertion compares against `canonicalize(expected)`. On macOS `std::env::temp_dir()` is under `/var/folders/...` and `/var` is a symlink to `/private/var`, so an assertion against a raw `ScratchDir` path passes on Linux and fails on macOS — the single most likely way for this suite to disagree with itself between the two CI legs
- [ ] 1.5 VERIFY: `cargo build --locked` exits 0 and `Cargo.lock` is byte-identical to HEAD's — this change adds no dependency, and a lock that moved means one was added by accident. Re-run the `$DEPS` filter from design.md → Test Strategy and confirm it still asserts exactly `["toml"]`. A filter ending in `print()` exits 0 for any dependency list and is not evidence
- [ ] 1.6 VERIFY: `cargo test --all-features` is still green with the new empty module declared — the skeleton compiles and nothing regressed

## 2. Repository discovery
<!-- kind: behavior -->

- [ ] 2.1 RED: Write failing unit tests in `src/resolve.rs` for `find_repo`, one per spec scenario, named after them: "The starting directory is itself the repository", "The repository is an ancestor several levels up", "The innermost repository wins", "A regular file named `openspec` is not a repository", "A symbolic link to a directory is a repository", "A dangling symbolic link named `openspec` is not a repository", "A starting path containing `..` is resolved before the walk", "A starting path naming a regular file is walked from its parent", "A starting directory that does not exist is not an error", "No repository anywhere up to the filesystem root". Build every fixture under a `ScratchDir`; never assert against a path in the real repository
- [ ] 2.2 RED: Make three of those tests discriminating rather than merely green-able, because each targets a specific wrong implementation. "The innermost repository wins" must assert the result is **not** the outer root, or a walk that runs to the outermost match passes. "A regular file named `openspec` is not a repository" must place a real directory in the parent, or an `exists()`-based walk passes. "A starting path containing `..` is resolved before the walk" must assert the returned string contains no `..`, or a lexical walk returning `R/a/..` passes
- [ ] 2.3 RED: For "No repository anywhere up to the filesystem root", assert the precondition **before** the call: walk the scratch directory's ancestors and assert none holds an `openspec` directory, with a message naming the offending ancestor. A stray `/openspec` or `/tmp/openspec` on some machine must fail this test loudly, not turn it into a silent pass or a skip
- [ ] 2.4 RED: Write the two containment tests in the same pass — "A repository tree is byte-identical after discovery" (snapshot paths, bytes, and modification times of a fixture holding `openspec/changes/x/tasks.md`, `openspec/specs/`, and `README.md` through `testutil::snapshot`, run discovery twice from `openspec/changes/x`, assert snapshot equality) and "A missing starting directory is not created" (discovery from an uncreated path; assert it and every intermediate directory are still absent and the scratch root's listing is unchanged). Read modification times through `std::fs::Metadata`, never by shelling out to `stat`, whose flags differ between BSD and GNU. Assert bytes and mtimes, not only the listing: a rewrite of identical length passes a listing-only check. Be honest about what these two are — **guards, not behaviour drivers**. Before `find_repo` exists they fail to compile, which is not the same as failing for the behaviour; what they will genuinely fail against is an implementation that creates a directory while walking
- [ ] 2.5 Confirm every failure is the missing function rather than a broken fixture — assert inside at least one test that the fixture directory exists before the call under test, and check that `cargo test --all-features resolve::` names the new tests rather than failing to compile for an unrelated reason
- [ ] 2.6 GREEN: Implement `find_repo(start: &Path) -> RepoSearch`. Canonicalize `start` when the filesystem can; walk the resulting path and every ancestor in order; accept the first whose `openspec` child is a directory *through* symlinks (`fs::metadata(...).map(|m| m.is_dir())`, never `symlink_metadata`); stop at the filesystem root. Return `Found { root }`, or `NotFound { searched_from }` naming the canonicalized path when canonicalization succeeded and the given path verbatim when it did not. Treat every `Err` from the filesystem as "not a repository" — no `unwrap`, no `expect`, no panic on an unreadable or non-existent path
- [ ] 2.7 REFACTOR: Extract the "is this path a directory, following links" predicate if `openspec_bin`'s usability check will share it; if the two turn out to need different shapes — one wants a directory, the other a regular file with an execute bit — say so in 2.8 rather than forcing a shared helper
- [ ] 2.8 Run the group tests — `cargo test --all-features` green, no regressions to the existing `config`, `state`, or invocation tests

## 3. Candidate usability and the `PATH` step
<!-- kind: behavior -->

- [ ] 3.1 RED: Write failing unit tests for the usability predicate and the `PATH` step, named after their spec scenarios: "An executable regular file is usable", "A file without an execute bit is skipped", "A directory named `openspec` is skipped", "A symbolic link to an executable file is usable and is returned unresolved", "A dangling symbolic link is skipped", "`PATH` entries are searched left to right", "An empty `PATH` entry is not the current directory". Supply `PATH` through the injected `&dyn Fn(&str) -> Option<String>` lookup over a fixture map — never `std::env::set_var`, which is `unsafe` in edition 2024 and races parallel tests, and never the machine's real `PATH`
- [ ] 3.2 RED: Each of the first three tests targets a distinct wrong implementation, and must fail against it: the non-executable case fails an `is_file()`-only check, the directory case fails a mode-bits-only check (a directory carries the execute bit), and the symlink case fails a `symlink_metadata` check. Write the fixtures so the *other* candidate is the expected answer, so a predicate that accepts everything returns the wrong path rather than the right one by luck
- [ ] 3.3 RED: "A symbolic link to an executable file is usable and is returned unresolved" must assert the result equals the link path **and** is not the target path. An implementation that canonicalizes its result passes an equality-only assertion on this machine roughly half the time, depending on which path the fixture built first
- [ ] 3.4 RED: "`PATH` entries are searched left to right" runs twice with the two directories swapped, so the test cannot pass by accident of creation order. "An empty `PATH` entry is not the current directory" uses `PATH=":D:"` and asserts the result is absolute and is not the bare relative path `openspec`
- [ ] 3.5 Confirm the failures are the missing predicate and missing step, not the fixture builders from 1.3 — assert in at least one test that a fixture file exists and has the mode the builder set, before the call under test
- [ ] 3.6 GREEN: Implement the usability predicate — `fs::metadata` (which follows symlinks), `is_file()`, and `mode & 0o111 != 0`. Every `Err` means "not usable", never a panic
- [ ] 3.7 GREEN: Implement the `PATH` step: read `PATH` through the injected lookup, split on `':'`, skip empty entries, join `openspec` to each remaining entry in order, and return the first usable candidate. An absent `PATH` yields no candidates. Do not use `std::env::split_paths`: it takes an `OsStr` while the lookup is `String`-typed, and on Unix it yields empty entries that would have to be filtered anyway — design.md → Decisions records the reasoning so it is not "simplified" back
- [ ] 3.8 REFACTOR: If the usability predicate and the candidate loop have grown duplicated `Err` handling, collapse it while the tests stay green; otherwise state that none was needed
- [ ] 3.9 Run the group tests — `cargo test --all-features` green, no regressions

## 4. The nvm step
<!-- kind: behavior -->

- [ ] 4.1 RED: Write failing unit tests named after their spec scenarios: "The nvm tree is searched when `PATH` has nothing", "Node versions are ordered numerically, not lexically", "A version directory without a usable binary is skipped", "A version directory whose name is not a version is still eligible", "`NVM_DIR` overrides the default nvm root". Every fixture is a scratch tree; `NVM_DIR` and `HOME` come from the injected lookup, so the user's real `~/.nvm` is never read. This is not hypothetical shielding — the reference machine holds `v24.18.0`, `v24.19.0`, and `v24.20.0`, **all three carrying `openspec`**
- [ ] 4.2 RED: "Node versions are ordered numerically, not lexically" uses `v9.99.99` and `v10.0.0` and asserts the `v10.0.0` path. A lexical `sort().rev()` returns `v9.99.99` and is the implementation this test exists to reject. "A version directory without a usable binary is skipped" places the answer under the **lowest** version (`v22.0.0` empty, `v21.0.0/bin/openspec` mode `0644`, `v20.0.0/bin/openspec` mode `0755`), so an implementation that stops at the first version directory rather than the first usable binary fails
- [ ] 4.3 RED: "`NVM_DIR` overrides the default nvm root" builds two distinct trees holding different executables and asserts the `NVM_DIR` one, then re-runs with `NVM_DIR = "   "` and asserts the `HOME` one — the blank-value rule `config::non_blank` already establishes for every other environment variable in this crate
- [ ] 4.4 Confirm the failures are the missing step rather than a mis-built tree — assert one of the fixture binaries exists and is executable before the call
- [ ] 4.5 GREEN: Implement the nvm step. Root is `NVM_DIR` when non-blank (reuse `config::non_blank` rather than re-deriving the rule), else `$HOME/.nvm`, else no candidates. Read `<root>/versions/node` with `read_dir`; an `Err` means no candidates. Sort entries newest first: parse a leading `v` plus a `MAJOR.MINOR.PATCH` triple of integers, order parsed versions descending, place unparseable names after every parsed one and order those by name descending. Try `<entry>/bin/openspec` for each in turn and take the first usable one — skipping a directory rather than ending the step
- [ ] 4.6 REFACTOR: If the ordering comparator has become dense, give it a named function and a doc comment stating the `v10.0.0` > `v9.99.99` rule and why; otherwise state that no refactor was needed
- [ ] 4.7 Run the group tests — `cargo test --all-features` green, no regressions

## 5. The chain, the configured step, and the deferred hook
<!-- kind: behavior -->

- [ ] 5.1 RED: Write failing unit tests for the assembled chain, named after their spec scenarios: "The configured path wins over every other source", "`PATH` wins when nothing is configured", "An absent or blank `PATH` contributes nothing", "The npm prefix is the last resort", "An npm prefix without a usable binary yields nothing", "Nothing anywhere is a supported state, not a fault", "A configured path that does not exist falls through to `PATH`", "A configured path that is not executable falls through", "A configured path fails and nothing else is found", "The shipped hook yields no prefix"
- [ ] 5.2 RED: Every ordering test asserts the reported `BinSource`, not only the path. On the reference machine steps 3 and 4 resolve to the *same* path (`npm prefix -g` prints the nvm version directory), so a path-only assertion cannot tell the two apart and would stay green with the steps swapped. "The configured path wins over every other source" additionally re-runs with the configured argument dropped and asserts the `PATH` answer, so the test is about the ordering rather than about one fixture
- [ ] 5.3 RED: The three `problems` tests are what make `problems` load-bearing, and each must assert the *count* as well as the content. "Nothing anywhere is a supported state, not a fault" asserts no binary **and** `problems.is_empty()`; "A configured path fails and nothing else is found" asserts no binary **and** exactly one problem containing the configured path. Without both halves the two cases are indistinguishable and an implementation that never records a problem — or one that records a problem whenever nothing is found — passes
- [ ] 5.4 RED: "The shipped hook yields no prefix" asserts `npm_prefix_deferred()` is `None`, and its assertion message names `subprocess-seam` as the change that will make it fail, because going red there is the intended hand-over signal rather than a regression. Do not write a test that calls `openspec_bin` with `npm_prefix_deferred` and asserts nothing was found: that is green against a chain that never reaches step 4 at all
- [ ] 5.5 RED: Write "A full probe leaves the filesystem byte-identical" — snapshot paths, bytes, and modification times of a scratch tree holding a `PATH` directory, an nvm tree, and an npm prefix; run a full four-step probe twice; assert snapshot equality and that no entry appeared. Assert mtimes, not only existence. Like the discovery containment tests, this is a **guard**: what it genuinely fails against is a probe that creates a directory, not the absence of the chain
- [ ] 5.6 Confirm the failures are the missing chain rather than the already-implemented steps — `cargo test --all-features resolve::` still passes the group 3 and group 4 tests while the new ones fail
- [ ] 5.7 GREEN: Implement `BinSource`, `FoundBin`, and `BinResolution`, and `openspec_bin(configured, env, npm_prefix)`. Step 1: when `configured` is `Some` and usable, return it with `BinSource::Configured`; when `Some` and not usable, push exactly one problem naming the path and continue. Steps 2, 3, 4 in order, each returning its own `BinSource`. When nothing is usable, return `found: None` with whatever problems step 1 produced — never a synthesised "not found" problem
- [ ] 5.8 GREEN: Implement `npm_prefix_deferred() -> Option<PathBuf>` returning `None`, with a doc comment stating that step 4 needs `npm prefix -g`, that `cli` does not exist until `subprocess-seam`, and that whoever wires it must read **stdout only** and trim it — `npm` writes unrelated shell-plugin noise to stderr on the reference machine. Do not write the words `Command`, `Stdio`, or `std::process` anywhere in `src/resolve.rs`, including in that comment: 7.1's module-scoped pattern matches source text and cannot tell a comment from a call
- [ ] 5.9 CHECK: Contract gate for the resolution chain — re-read `SPEC.md` → Data layer → Resolution chain, "The `openspec` binary", as it stands at implementation time, and confirm the four steps, their order, and their sources still match what `openspec_bin` implements. It currently under-specifies executability, symlinks, nvm ordering, and a mis-configured value; group 9 owns those corrections. This gate confirms nothing *else* drifted — in particular that no other change has since renumbered or reordered the steps
- [ ] 5.10 REFACTOR: The four steps are four small functions returning `Option<PathBuf>` plus a chain that labels each with its `BinSource`. If they have instead grown into one long function, split them now while the tests stay green; otherwise state that no refactor was needed
- [ ] 5.11 Run the group tests — `cargo test --all-features` green, no regressions

## 6. Session cache and the one real-environment composition
<!-- kind: behavior -->

- [ ] 6.1 RED: Write failing unit tests named after their spec scenarios: "A second lookup does not re-probe", "A negative result is cached too", "Two caches are independent". Each uses a probe closure that increments an `AtomicUsize` and returns a fixed resolution; the counter is read **after** both calls have returned. No sleeping, no polling, no clock — nothing here is timing-shaped, and a test that waited for a cache to warm would be asserting that something has already happened
- [ ] 6.2 RED: "A second lookup does not re-probe" asserts the counter is exactly 1, which fails against a `get_or_probe` that just calls `probe()`. "A negative result is cached too" uses a resolution with no binary, which is the case a naive `if let Some(...)` cache re-probes forever. "Two caches are independent" is what would fail if the cache were a `static`, and it is the reason the cache is a value — record that in the test's own comment
- [ ] 6.3 RED: Write "The composition honours a configured binary" — call `openspec_bin_from_env` with a `Config` whose `openspec_bin` is a scratch executable, and assert that exact path with `BinSource::Configured`. It is deterministic because step 1 short-circuits before the real `PATH`, `HOME`, and `NVM_DIR` are read, and discriminating because no other step could produce a scratch path. Do **not** write the tautological form `openspec_bin_from_env(c) == openspec_bin(c.openspec_bin, &env_lookup(), &npm_prefix_deferred)`: both sides call the same functions on the same environment, so it passes against a composition that ignores its argument entirely — the exact defect `plugin-config`'s review found in `load_from_env`
- [ ] 6.4 Confirm the failures are the missing cache and missing composition, not a mis-built `Config`
- [ ] 6.5 GREEN: Implement `BinCache` as a `OnceLock<BinResolution>` with `get_or_probe(&self, probe: impl FnOnce() -> BinResolution) -> &BinResolution` delegating to `OnceLock::get_or_init`, and `#[derive(Default)]`. Do not add a `static` instance anywhere; `tui-shell` will own one value for the process
- [ ] 6.6 GREEN: Implement `openspec_bin_from_env(config: &Config) -> BinResolution` as `openspec_bin(config.openspec_bin.as_deref(), &config::env_lookup(), &npm_prefix_deferred)` and nothing else — one composition, so the crate's untestable residue does not grow
- [ ] 6.7 REFACTOR: Clean up while green, or state that no refactor was needed
- [ ] 6.8 Run the group tests — `cargo test --all-features` green, no regressions

## 7. No-spawn and dependency confirmation
<!-- kind: operational -->

- [ ] 7.1 CHECK: Before running anything, write down what each check below is evidence *about* and where it stops, so nothing here is overclaimed in a commit message or inherited as settled by a later change. 7.2 is evidence about **source text**: it would not catch a spawn reached through a dependency. 7.3 is evidence that the **test suite** needs none of `npm`, `node`, or `openspec`: it is not proof that production code never spawns, because a spawn whose error is swallowed would still pass. 7.4 closes the dependency route. The three together, plus a step-4 collaborator that is a closure with exactly one production binding returning `None`, are the whole argument
- [ ] 7.2 VERIFY: Nothing spawns, in source text — the `SPAWN` block in design.md → Test Strategy, both lines. Tree-wide: `! grep -rnE 'process::Command|Command::new|Stdio' src/`. Module-scoped and stricter: `! grep -nE 'std::process|Command|Stdio' src/resolve.rs`. Verified on this tree before planning: the tree-wide form is clean at HEAD and fires on a planted `use std::process::Command;`. Do **not** add `! grep -rn '"openspec"' src/`, the program-name-literal check `plugin-config` used — it is **already false at HEAD** (`src/state.rs:653` joins the literal to build a fixture path) and this change adds more such joins, so reintroducing it would force someone to weaken the checks that do work — spec scenario "Resolution spawns no process"
- [ ] 7.3 VERIFY: Nothing spawns, at run time — the `NOSPAWN-RUN` block. Build `NOTOOLS` by testing each `PATH` entry with `[ -x "$d/npm" ]`, `[ -x "$d/node" ]`, `[ -x "$d/openspec" ]` and dropping the ones that match. Then **assert all three are unresolvable on it and that `cargo` still is, before running the suite** — without those four assertions the run proves nothing. Three traps, each verified on this machine: `dirname "$(command -v npm)"` yields `.` here because `npm` is a shell function, so a `command -v`-based recipe removes nothing; removing only the nvm bin directory leaves `npm` and `node` resolvable via Homebrew; and `env PATH=/usr/bin:/bin cargo ...` execs through the new `PATH` and cannot find cargo at all. Do not use `paste -sd:` to rebuild a `PATH` — on BSD it needs a file operand and silently yields an empty string. Record which form was used — same spec scenario
- [ ] 7.4 VERIFY: No dependency was added — `cargo metadata --no-deps --format-version 1 | python3 -c "$DEPS"` asserts the normal-kind dependency list is still exactly `["toml"]`, and `Cargo.lock` is byte-identical to HEAD's. This is the live `plugin-build` requirement, not a scenario of this change; it is checked rather than assumed because a `glob` or `which` crate would have been the easy way to write steps 2 and 3, and `which` shells out on some platforms
- [ ] 7.5 VERIFY: No scratch state leaked — no `herdr-openspec-test-` directory (the `ScratchDir` name shape) remains under `std::env::temp_dir()` after the suite. One that does means a test panicked and the helper's drop did not run

## 8. Change Review
<!-- kind: operational -->

- [ ] 8.1 CHECK: Dispatch an independent reviewer — not a fork of the implementing session — against proposal.md, both spec files, design.md, and tasks.md plus the diff. Point it first at the concentration points that bite here: every ordering assertion checking `BinSource` and not only a path; the three usability tests actually failing against the three wrong predicates named in 3.2; the symlink test rejecting a canonicalizing implementation; the two `problems` tests distinguishing "nothing installed" from "configured wrong" by count; the cache tests failing against no cache and against a `static`; `openspec_bin_from_env`'s test not being the tautological form; the containment tests asserting bytes and mtimes rather than listings; and every discovery assertion comparing against `canonicalize(expected)` so macOS and Linux agree
- [ ] 8.2 CHANGE: Fix every CRITICAL, resolve or consciously accept each WARNING with a one-line reason, note SUGGESTIONs, and re-run every affected check
- [ ] 8.3 VERIFY: Confirm no blocking or unowned finding remains, and that every artifact a repair touched was updated in place rather than annotated afterwards

## 9. Documentation
<!-- kind: operational -->

- [ ] 9.1 CHECK: Re-read, at implementation time rather than from memory of this plan, the exact current wording of every section rewritten below: `SPEC.md` → Architecture (the subprocess-seam paragraph, including the "untestable residue" sentence), → Data layer → Resolution chain (both "Repository" and "The `openspec` binary"), → Degraded states, → Testing and quality gates → Unit-tested modules; `openspec/IMPLEMENTATION-ORDER.md` → the Phase 3 `subprocess-seam` row; `AGENTS.md` → Current repo state. Record what each says now, so a correction another change already made is neither duplicated nor reverted
- [ ] 9.2 CHANGE — rewrite in `SPEC.md`: Architecture → the subprocess seam (audience: `subprocess-seam`, which is planned from this paragraph) — it says "All process spawning sits behind two traits", `OpenspecCli` and `HerdrCli`. Step 4 of the binary chain spawns `npm`, which is neither, so as written the specification either forbids step 4 or forces `npm` to masquerade as one of the two. State the invariant that is actually load-bearing — `cli` is the only module that spawns — and name the one-shot `npm prefix -g` probe as a third thing living there, injected into `resolve` as a `&dyn Fn() -> Option<PathBuf>` so `resolve` stays pure. Extend the "untestable residue is two thin wrappers and `main`" enumeration in the same pass. This is a rewrite of the existing paragraph, not a fourth bullet beside it
- [ ] 9.3 CHANGE — rewrite in `SPEC.md`: Data layer → Resolution chain → "The `openspec` binary" (audience: `changes-from-cli` and `subprocess-seam`) — the four steps are listed with no statement of what counts as a hit. Add: a step matches only an executable regular file reached through symlinks; the returned path is not canonicalized; step 3's root is `NVM_DIR` when set and `$HOME/.nvm` otherwise, with version directories tried newest-first by numeric comparison so `v10.0.0` precedes `v9.99.99`; a configured `openspec_bin` that is not usable falls through to the remaining steps and is recorded as a problem rather than winning or ending the chain; and step 4 is unwired until `subprocess-seam`, so it yields nothing today
- [ ] 9.4 CHANGE — add to `SPEC.md`: Data layer → Resolution chain → "Repository" (audience: `changes-from-files`, which joins onto this root) — one clause each for the three rules the current two sentences leave open: the innermost `openspec/` wins, a regular file of that name does not count, and the root is canonicalized so a `..` component or a symlinked working directory does not reach the empty state or a joined path
- [ ] 9.5 CHANGE — add to `SPEC.md`: Degraded states table (audience: `degraded-states`, which is planned from this table) — one row: a configured `openspec_bin` that does not name an executable falls through to the remaining probe steps, and the fallback is named in the resolution's `problems` rather than being silent. The existing "`openspec` binary not found → file mode" row already covers the nothing-found case and needs no edit; check that it does not now contradict the new row
- [ ] 9.6 CHANGE — rewrite in `SPEC.md`: Testing and quality gates → Unit-tested modules (audience: future changes choosing a tier) — the list names `resolve::openspec_bin` and omits the repository walk, which is half of `resolve` and has twelve scenarios of its own. Add it in the same entry rather than as a second bullet
- [ ] 9.7 CHANGE — rewrite in `openspec/IMPLEMENTATION-ORDER.md`: the Phase 3 `subprocess-seam` row (audience: whoever implements it) — its scope says "the two traits, one real implementation each, and the fakes". Add the `npm prefix -g` probe and the obligation to replace `resolve::npm_prefix_deferred`, so the hand-over is recorded on the roadmap rather than living only in a doc comment. Confirm the dependency graph needs no edge change: `repo-resolution` correctly does **not** depend on `subprocess-seam`, because the hook it ships is a pure function
- [ ] 9.8 CHANGE — rewrite in `AGENTS.md`: Current repo state (audience: a fresh session's first paragraph) — it says "Nothing consumes that configuration yet — `repo-resolution` is next", which this change makes false. Replace with what is true after it: the crate resolves the repository root and the `openspec` binary, and `changes-from-files` is next. This is a rewrite of the existing sentence, not an addition
- [ ] 9.9 CHECK: Consider and reject, or make, one `AGENTS.md` → Architecture rules edit. The spawn bullet already says "Nothing spawns a process outside `cli`", which stays true. The only candidate rule this change establishes is "a collaborator that does not exist yet is an injected closure, not an early seam" — decide whether that is a durable constraint a future change would get wrong without it, or a decision already recorded in this change's design.md where it belongs. `AGENTS.md` is a fixed attention budget; prefer not adding
- [ ] 9.10 VERIFY: Net size — this group rewrites five entries in place across three documents and adds roughly ten lines. Confirm no entry it touched now says something false: that all spawning sits behind two traits, that the binary chain is four unqualified path guesses, that the repository walk has no tie-break, or that nothing consumes plugin configuration

## 10. Lint & Verify
<!-- kind: operational -->

- [ ] 10.1 CHECK: Inspect the intended verification commands and affected tiers — the unit tier (`cargo test --all-features resolve::`, plus the whole suite for regressions), the command checks in groups 1 and 7, and coverage, which is affected: this change adds a module of comparable size to `src/config.rs`
- [ ] 10.2 VERIFY: `cargo fmt --all -- --check` — clean
- [ ] 10.3 VERIFY: `cargo clippy --all-targets --all-features -- -D warnings` — 0 errors. Probe with `cargo clippy --version`, not `command -v cargo-clippy`: rustup installs that shim unconditionally, so the shim resolves even when the component is absent
- [ ] 10.4 VERIFY: `cargo test --all-features` — green, including the unchanged `config` and `state` unit tests and the `tests/cli.rs` and `tests/ci_workflow.rs` integration tests, none of which this change touches. Rust has no separate type-check step; `cargo test` and `cargo clippy` cover it
- [ ] 10.5 VERIFY: `cargo llvm-cov --fail-under-lines 80` — at or above the floor, and record the resulting percentage. If it falls short, add tests; never lower the threshold, never add an exclusion flag, and never mark a function `#[cfg(not(test))]` to hide it from the count
- [ ] 10.6 VERIFY: `make check` — the single composite gate, exit 0. If it fails, name the failing sub-command rather than reporting a summary
- [ ] 10.7 VERIFY: `openspec validate repo-resolution --strict` reports the change valid, with nvm ahead on `PATH` — `export PATH="$HOME/.nvm/versions/node/v24.20.0/bin:$PATH"` — since `openspec` is not on the `PATH` a non-login shell inherits here
