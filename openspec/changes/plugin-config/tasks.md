<!-- No outer-loop acceptance group: design.md → Test Strategy records why. The change's
     outermost surface is a library API — `src/main.rs` is untouched and no caller
     consumes `Config` until `repo-resolution` — so an acceptance test would drive an
     entry point that ships to nobody. The unit tier reaches every collaborator these
     functions have; the one it cannot reach, Herdr's injected environment, is the live
     check in group 6.

     All nine concentration points from openspec/config.yaml are accounted for. Three do
     not apply and their absence is deliberate: no view is added, so "views perform no
     I/O" and "view tests run at 60 and 120 columns" have nothing to bind to; the
     `Change` type does not exist yet, so `from_files`/`from_cli` agreement is not at
     stake. One is not applicable for a narrower reason: "agent attribution must not
     guess" — no attribution path exists here, `agent-attribution` owns it, and this
     change's `read` deliberately *skips* an entry Herdr would reject rather than
     repairing or guessing at it. The remaining five are scheduled: no spawn outside
     `cli` (6.2, 6.3), nothing written inside `openspec/` (5.2, 5.9), an absent case for
     every external dependency (2.1, 3.1, 5.1), the 32-character agent-name cap (4.1),
     and "coverage counts the whole crate" (2.4 confines the untestable residue to one
     composition, 2.6 gives the one real-environment binding assertions of its own, and
     9.5 holds the floor).

     None of the command checks in this file may be committed as a Rust test. A test
     that reads `Cargo.toml` and asserts `toml` is listed proves only that the string was
     typed twice. -->

## 1. Dependency and module skeleton
<!-- kind: operational -->

- [x] 1.1 CHECK: Confirm the crate has no third-party dependency today — `cargo metadata --no-deps --format-version 1` reports an empty dependency list and `Cargo.lock` holds exactly one `[[package]]` — and that `make check` is green at HEAD, so any later failure belongs to this change
- [x] 1.2 CHECK: Read the current stable `toml` version from the registry rather than from this file — `cargo info toml` — and record what it reports. `1.1.5` (MSRV 1.85, matching `Cargo.toml`'s `rust-version`) is what design.md argued. If the registry has moved, take the newer patch or minor, confirm its reported `rust-version` is still at or below `1.85`, and update the version cell in `specs/plugin-build/spec.md`'s dependency table in the same commit — the spec says "at least 1.1.5", so a bump is not a spec edit unless the MSRV moves, and a raised MSRV is a decision to record, not to absorb
- [x] 1.3 CHANGE: Add to `[dependencies]`: `toml = { version = "<recorded version>", default-features = false, features = ["std", "parse", "display", "serde"] }`. All four features are load-bearing — `parse` reads, `display` writes `agent-names.toml`, `serde` gates `toml::Table` itself, `std` gates the rest — and they happen to be the crate's own defaults today, so naming them prunes nothing now and makes a future default change a reviewable diff
- [x] 1.4 CHANGE: Add empty `src/config.rs` and `src/state.rs` and declare `pub mod config;` and `pub mod state;` in `src/lib.rs`, leaving the existing invocation-parsing code untouched
- [x] 1.5 CHANGE: Add a `#[cfg(test)]` test helper (in `src/lib.rs` or a `testutil` module) that creates a directory named `herdr-openspec-test-<pid>-<counter>` under `std::env::temp_dir()` and removes it on drop. Derive the counter from a `static AtomicUsize`, not from the clock and not from a random source, so a test can predict the paths it will see and 6.5 has a name to search for. No `tempfile` dependency — design.md → Decisions argues why
- [x] 1.6 VERIFY: `cargo build --locked` exits 0 with the regenerated `Cargo.lock` committed, and the resolved metadata says the normal-kind dependency list is exactly `["toml"]`, with `uses_default_features` false and features exactly `display`, `parse`, `serde`, `std` — spec scenario "The declared dependency set is exactly one crate". Use the asserting filter from design.md → Test Strategy (`$DEPS`); a filter that ends in `print()` exits 0 for any dependency list and is not evidence
- [x] 1.7 VERIFY: `cargo tree -e normal` names exactly `herdr-openspec`, `toml`, `serde_core`, `serde_spanned`, `toml_datetime`, `toml_parser`, `toml_writer`, `winnow`, and none of `syn`, `quote`, `proc-macro2`, `serde_derive` — spec scenario "The resolved build graph is small and proc-macro-free". `Cargo.lock` will hold more entries than that; those are optional resolutions cargo never builds, which is why no check counts lock entries
- [x] 1.8 VERIFY: Exactly one target of kind `bin`, named `herdr-openspec` (the `$BIN` filter from design.md), **and** `/bin/sh scripts/build.sh && test -x target/release/herdr-openspec` — spec scenario "Exactly one binary target is produced at the release path". Run both halves: metadata cannot fail if the release build breaks on the new dependency, which is the half worth checking here

## 2. Configuration directory resolution
<!-- kind: behavior -->

- [x] 2.1 RED: Write failing unit tests in `src/config.rs` for `config_dir`, taking a `&dyn Fn(&str) -> Option<String>` over a fixture map — never `std::env::set_var`, which is `unsafe` in edition 2024 and races parallel tests. Cover spec scenarios "Herdr supplies the directory" (variable wins; identical result with `HOME` absent), "Run outside a Herdr pane" (`$HOME/.config/herdr/plugins/config/herdr-openspec`), "An empty or blank environment variable is not a directory" (`""` and `"   "` both fall through), "`XDG_CONFIG_HOME` is deliberately ignored" (the `HOME` path wins over `/xdg`), and "Neither variable is available" (`None`)
- [x] 2.2 Confirm the failures come from the missing function, not a broken fixture — `cargo test --all-features config::` names those tests and none fails to compile the harness for an unrelated reason
- [x] 2.3 GREEN: Implement `config::config_dir`. Read `HERDR_PLUGIN_CONFIG_DIR` first, treating an empty or whitespace-only value as unset; otherwise join `HOME`; otherwise `None`. Do not consult `XDG_CONFIG_HOME`. No filesystem access, no process API
- [x] 2.4 GREEN: Implement `config::load_from_env` as `load(config_dir(&env_lookup()), &env_lookup())` and nothing else — one composition, so the crate's untestable residue does not grow
- [x] 2.5 RED: Write failing unit tests for `config::env_lookup` that can distinguish a real lookup from a stub: `env_lookup()("PATH")` equals `std::env::var("PATH").ok()` and is `Some`, and `env_lookup()("HERDR_OPENSPEC_DEFINITELY_UNSET_<unique>")` is `None`. Do **not** write a test comparing `load_from_env()` with `load(config_dir(&lookup), &lookup)` for a lookup built from the real environment: both sides call the same function on the same variables, and on any machine without a real `config.toml` both are `Config::default()`, so it passes against a `load_from_env` that returns the default and ignores the environment entirely
- [x] 2.6 GREEN: Implement `config::env_lookup` as the single binding of a name to `std::env::var`, returning `None` for both `NotPresent` and `NotUnicode`
- [x] 2.7 REFACTOR: Extract the shared "read a variable, treat empty or whitespace-only as unset" helper if `state` will want it too; if nothing warrants changing, say so in 2.8
- [x] 2.8 Run the group tests — `cargo test --all-features` green, no regressions to the existing invocation tests

## 3. Reading `config.toml`
<!-- kind: behavior -->

- [x] 3.1 RED: Write failing unit tests in `src/config.rs` for `load(dir, env)` against a scratch directory, one per spec scenario, with names derived from them: "Every key is set", "The file does not exist", "The directory does not exist", "An empty file is not a malformed file", "Only one key is set", "Unrecognised keys are ignored", "The file is not valid TOML", "One key has the wrong type, the rest survive", "A negative count is not a count", "The file cannot be read", "A tilde path is expanded", "A `$HOME` path is expanded and a bare tilde is the home directory", "An unexpandable value is returned verbatim", "An empty value is an absent value", "A path that does not exist is still returned", "A configuration read leaves the tree byte-identical", "A missing configuration directory stays missing". The empty-file and malformed-file tests must assert on `problems` as well as on the values — that assertion is the only thing distinguishing them, and without it both are green whichever way the code goes
- [x] 3.2 RED: For "The file cannot be read", create `config.toml` as a *directory*; for the byte-identical test, snapshot the listing, each file's bytes, and each file's modification time before and after two loads and assert no entry, no byte, and no mtime moved and no temporary file appeared. Read mtimes through `std::fs::Metadata`, never by shelling out to `stat`, whose flags differ between BSD and GNU
- [x] 3.3 Confirm every failure is the missing behaviour rather than a scratch-directory helper that never created anything — assert the fixture exists inside at least one test before the call under test
- [x] 3.4 GREEN: Implement the `Config` type — `openspec_bin: Option<PathBuf>`, `agent_kind: String`, `archived_count: usize`, `problems: Vec<String>` — with a `Default` giving absent, `claude`, `5`, and no problems
- [x] 3.5 GREEN: Implement `load`. Return `Config::default()` for `None`, a missing directory, or a missing file, with no problem recorded. Read the file; a read error or a TOML parse error yields full defaults plus one problem naming `config.toml`. Per key, a value of the wrong TOML type — and a negative `archived_count` — yields that key's default plus one problem naming the key, while the keys that parsed stay honoured. Ignore unknown keys and tables silently. Never return `Result`, never panic
- [x] 3.6 GREEN: Implement `openspec_bin` expansion: trim, treat empty as absent, expand a leading `~/` and a leading `$HOME/` and a bare `~` using the same injected lookup, return the value verbatim when `HOME` is unavailable or the value is a `~user` form, and never touch the filesystem to check the result
- [x] 3.7 CHECK: Contract gate for the config format — re-read `SPEC.md` → Data layer → Resolution chain and `README.md` → Configuration and confirm the three key names, their types, and their defaults still match what `load` implements. They currently do not agree with the implementation on where the directory comes from; group 8 owns that correction, and this gate confirms nothing *else* drifted
- [x] 3.8 REFACTOR: Collapse the three per-key readers into one typed accessor if that removes real duplication while the tests stay green; otherwise state that none was needed
- [x] 3.9 Run the group tests — `cargo test --all-features` green, no regressions

## 4. State directory and agent-name derivation
<!-- kind: behavior -->

- [x] 4.1 RED: Write failing unit tests in `src/state.rs` for `state_dir` and `agent_name`, named after the spec scenarios: "Herdr supplies the state directory" (and that it differs from `config_dir` on the same lookup), "`XDG_STATE_HOME` is honoured before `HOME`", "Run outside a Herdr pane with no XDG setting" (including `XDG_STATE_HOME=""` and `"   "`), "No directory can be resolved at all", "A short kebab-case change name is unchanged", "A name of exactly 32 characters is not truncated", "A long change name is truncated with a suffix from the whole name", "A truncated name may be shorter than 32 characters", "Illegal characters and casing are normalised", "A name that cannot begin an agent name is prefixed", "A name with nothing usable in it". Assert the **exact literals** the spec names — `add-really-long-change-name-mmky`, `add-really-long-change-name-8jqt`, `add-really-long-change-name-alft`, `abcdefghijklmnopqrstuvwxyz-lhun` — not just a length and a regex: the literals are what pin the hash reduction, which is written to disk and must not vary between implementations. Every derivation test also asserts the result matches `^[a-z][a-z0-9_-]{0,31}$`, and the ≤32 assertion is "at most", never "exactly"
- [x] 4.2 Confirm the failures are the missing functions, not the regex or the fixture lookup
- [x] 4.3 GREEN: Implement `state::state_dir` — `HERDR_PLUGIN_STATE_DIR`, else `$XDG_STATE_HOME/herdr/plugins/herdr-openspec`, else `$HOME/.local/state/herdr/plugins/herdr-openspec`, else `None`; empty and whitespace-only values treated as unset throughout
- [x] 4.4 GREEN: Implement `state::agent_name` as a pure, total function of the change name alone, in the order design.md and the spec fix: ASCII-lowercase; every character outside `[a-z0-9_-]` becomes `-`; runs of `-` collapse and leading and trailing `-` and `_` are trimmed; an empty result becomes `change`; a first character outside `[a-z]` gains a `c-` prefix; a result longer than 32 characters becomes its first 27 characters with trailing separators trimmed, `-`, and four zero-padded lowercase base-36 digits of `fnv1a32(original) % 36^4`. State the reduction in the code as well as in the test: a `u32` renders as up to seven base-36 digits, so "first four" and "last four" are different names for the same change, and only one of them is this contract
- [x] 4.5 GREEN: Implement the FNV-1a-32 hash inline. Do not use `DefaultHasher`: its output is explicitly not stable across Rust releases and this value is written to disk
- [x] 4.6 REFACTOR: Remove duplication between `config_dir` and `state_dir`'s "first non-blank variable wins" logic if group 2 did not already; otherwise state that none was needed
- [x] 4.7 Run the group tests — `cargo test --all-features` green, no regressions

## 5. Reading and recording the mapping file
<!-- kind: behavior -->

- [ ] 5.1 RED: Write failing unit tests in `src/state.rs` for `read` and `record` against scratch directories, named after the spec scenarios: "Absent file and absent directory are both empty, not faults" (all three sub-cases), "A malformed file is empty and reports one problem", "A bad entry is skipped and its neighbours survive", "`names` is present but is not a table", "A truncated name is recorded", "An unchanged name is not recorded", "Recording the same pair twice changes nothing", "A second mapping is added beside the first", "The same agent name recorded for a different change replaces it", "The state directory is created on first record", "The file is replaced by rename, not written in place", "Recording fails without panicking". In the bad-entry fixture, **quote the two keys that need quoting** — `Bad_Name = "another-change"` is a legal TOML bare key that fails the agent-name pattern, but `has spaces! = …` unquoted makes the whole file a syntax error, which would exercise the malformed-file path instead of the per-entry path and contradict the sibling scenario
- [ ] 5.2 RED: Write the two containment tests — "A repository tree is untouched by a recording" (build a scratch fixture holding `openspec/changes/x/tasks.md`, snapshot listing, bytes, and mtimes, record into a *separate* scratch state directory, assert nothing in the fixture moved) and "The configuration directory is not written to" (the same three assertions, not just the listing: an in-place rewrite of identical length must not pass)
- [ ] 5.3 RED: Make "The file is replaced by rename, not written in place" able to fail. Take a `std::fs::hard_link` of the existing `agent-names.toml` to a second path in the same directory *before* recording; afterwards assert the target holds the new mapping **and the link still holds the previous bytes**. That is true of a rename and false of every in-place write. Do not use a content-only assertion or "no `.tmp-` entry remains": `std::fs::write` truncates, so it leaves the new bytes with no tail and creates no temporary file — it passes both, and it is exactly the non-atomic implementation the requirement forbids
- [ ] 5.4 Confirm each failure comes from the missing behaviour, not from a scratch directory the helper failed to create
- [ ] 5.5 GREEN: Implement `state::read` — `None`, a missing directory, a missing file, and a zero-byte file each give an empty `Mapping` with no problem. A parse failure, or a `names` key that is not a table, gives an empty mapping plus one problem. Per entry, a non-string value or a key that fails `^[a-z][a-z0-9_-]{0,31}$` is skipped with one problem, and the well-formed entries in the same file are still returned. Never `Result`, never panic
- [ ] 5.6 GREEN: Implement `state::record(dir, agent, change)`. The `agent == change` short-circuit returns `Ok(())` **before** the directory is consulted, so an unchanged name succeeds even with no state directory; otherwise `None` is an `Err`. Read the existing mapping, insert — an agent name already bound to a different change is rebound, deliberately and without a problem, because the most recent launch is the live one — and write. Serialise through `toml::Table` and its `Display`, not by formatting strings by hand, so a change name needing quoting cannot corrupt the file
- [ ] 5.7 GREEN: Make the write atomic — create the state directory, write the full contents to `agent-names.toml.tmp-<pid>-<counter>` **inside that same directory** so the rename cannot cross a filesystem boundary, then `std::fs::rename` over the target. On any failure return `Err` naming the path and remove the temporary file; never panic
- [ ] 5.8 CHECK: Persistence gate — `agent-names.toml` is the change's only stored data. Confirm no migration, backfill, seeding, cache invalidation, or index rebuild applies: there is no prior format, an absent file is a legitimate empty mapping, and nothing caches the read. Record that here rather than omitting it
- [ ] 5.9 CHECK: Contract gate — re-inspect the published surface (`Config`, `Mapping`, `config_dir`, `state_dir`, `load`, `env_lookup`, `load_from_env`, `agent_name`, `read`, `record`) against design.md → Contracts and confirm each named consumer still gets what that section promises it: `repo-resolution`, `changes-from-files`, `agent-launch`, `agent-attribution`, `degraded-states`. Confirm in particular that `record`'s documented order of operations matches the implementation. Nothing consumes them yet, so a drift here is silent until Phase 2
- [ ] 5.10 REFACTOR: Share the "read a TOML table, collect problems" shape between `config::load` and `state::read` if that is real duplication rather than coincidence; otherwise state that it is coincidence and leave them apart
- [ ] 5.11 Run the group tests — `cargo test --all-features` green, no regressions

## 6. Environment and dependency confirmation
<!-- kind: operational -->

- [ ] 6.1 CHECK: Confirm the live-Herdr premise before trusting it further, for **both** entrypoint kinds. Outside the repository, in a scratch directory, create two throwaway plugins whose ids are explicitly not `herdr-openspec` — one declaring a single `[[panes]]` entry, one a single `[[actions]]` entry — each with `id`, `name`, `version`, `min_herdr_version`, `platforms`, and a command of `/bin/sh -c 'env | sort > <scratch>/env-<kind>.txt'`. Link each, exercise it (`herdr plugin pane open --plugin <id> --entrypoint <id> --placement split --direction down --no-focus`; `herdr plugin action invoke <action-id> --plugin <id>`), wait for the file rather than assuming it, then `herdr plugin unlink <id>`. Assert `HERDR_PLUGIN_CONFIG_DIR` and `HERDR_PLUGIN_STATE_DIR` are present in **both** dumps and that the first is character-identical to `herdr plugin config-dir <id>`. Record `herdr --version` and both variables' values in planning-review.md, so the archive carries the proof rather than the claim. Then remove the two directories Herdr created under the user's real `~/.config/herdr/plugins/config/` and `~/.local/state/herdr/plugins/` — by the exact path `herdr plugin config-dir <id>` printed, never by a glob and never for the id `herdr-openspec`. **If this fails, stop:** the whole design rests on it, and the fallback paths become the only path — record the failure here and in planning-review.md rather than proceeding
- [ ] 6.2 VERIFY: Nothing spawns — the `SPAWN` block in design.md → Test Strategy, all four lines. Tree-wide: `! grep -rnE 'process::Command|Command::new|Stdio' src/`. Module-scoped, stricter: `! grep -nE 'std::process|Command|Stdio' src/config.rs src/state.rs`. Plus `! grep -rn '"herdr"' src/` and `! grep -rn '"openspec"' src/` for a program-name literal. Two traps to avoid, both verified against the real tree: forbidding `std::process` tree-wide fires on `src/main.rs`'s legitimate `std::process::exit`, and matching the bare word `herdr` fires on the accurate doc comment naming `herdr plugin config-dir` that this change deliberately writes — either would invite weakening the only automated proof of the repository's central invariant — spec scenario "Resolution spawns nothing"
- [ ] 6.3 VERIFY: The suite passes with `herdr` unreachable. Build the `PATH` by removing only the directory that holds `herdr` — `HERDR_DIR=$(dirname "$(command -v herdr)"); NOHERDR=$(printf '%s' "$PATH" | tr ':' '\n' | grep -vxF "$HERDR_DIR" | tr '\n' ':')` — first assert `! env PATH="$NOHERDR" command -v herdr`, then run `env PATH="$NOHERDR" cargo test --all-features`. Do **not** use `env PATH="/usr/bin:/bin" cargo test`: `env` execs through the new `PATH`, and cargo lives in `~/.cargo/bin`, so it exits 127 for a reason unrelated to the behaviour — the same defect `repo-foundation`'s review repaired. Do not use `paste -sd:` to rebuild a `PATH` either; on BSD it needs a file operand and silently yields an empty string. Same spec scenario; record which form was used
- [ ] 6.4 VERIFY: The dependency is load-bearing — copy **both** `Cargo.toml` and `Cargo.lock` aside to a scratch path, delete the `toml` line, confirm `cargo build` exits non-zero (expected, so do not run the sequence under `set -e`), restore both files, then confirm `cargo build --locked` exits 0 and `Cargo.lock` is byte-identical to the copy — spec scenario "The dependency is genuinely needed rather than incidental". Restoring only `Cargo.toml` leaves the lock the failed build rewrote, which breaks the very requirement this check is evidence for. Restore from the copies, never `git checkout --`: this change's own work may not be committed yet
- [ ] 6.5 VERIFY: No scratch state leaked — `~/.config/herdr/plugins/config/herdr-openspec` and `~/.local/state/herdr/plugins/herdr-openspec` are both unchanged by the test run, the two probe ids' directories from 6.1 are gone, and no `herdr-openspec-test-` directory (the helper's name shape from 1.5) remains under `std::env::temp_dir()`. One that does means a test panicked and the helper's drop did not run

## 7. Change Review
<!-- kind: operational -->

- [ ] 7.1 CHECK: Dispatch an independent reviewer — not a fork of the implementing session — against proposal.md, all four spec files, design.md, and tasks.md plus the diff. Point it first at the concentration points that bite here: a test asserting a fixture back at itself rather than the behaviour; the empty-file and malformed-file cases being distinguishable only by `problems`; the rename test actually failing against an `fs::write` implementation; `env_lookup`'s tests distinguishing a real lookup from a stub; the containment tests asserting mtimes rather than existence; and the agent-name tests pinning the exact literals rather than only a length and a regex
- [ ] 7.2 CHANGE: Fix every CRITICAL, resolve or consciously accept each WARNING with a one-line reason, note SUGGESTIONs, and re-run every affected check
- [ ] 7.3 VERIFY: Confirm no blocking or unowned finding remains, and that every artifact a repair touched was updated in place rather than annotated afterwards

## 8. Documentation
<!-- kind: operational -->

- [ ] 8.1 CHECK: Re-read, at implementation time rather than from memory of this plan, the exact current wording of every section the tasks below rewrite: `SPEC.md` → Overview (stack list), → Architecture (module map, and the "the untestable residue is two thin wrappers and `main`" sentence), → Data layer → Resolution chain, → Degraded states, → Herdr integration → Attributing an agent; `README.md` → Configuration; `AGENTS.md` → Current repo state, → Architecture rules, → Conventions; `openspec/IMPLEMENTATION-ORDER.md` → Phase 1 table. Record what each says now, so a correction another change already made is not duplicated or reverted
- [ ] 8.2 CHANGE — rewrite in `SPEC.md`: Data layer → Resolution chain, step 1 of "The `openspec` binary" (audience: every future change reading plugin configuration) — it says the directory is "reported by `herdr plugin config-dir herdr-openspec`", which no plugin process can call without a spawn. Replace with `HERDR_PLUGIN_CONFIG_DIR`, naming the command as the equivalent a human runs and the computed path as the fallback outside a Herdr-started process, and name `HERDR_PLUGIN_STATE_DIR` as the home of the agent-name mapping — the current text puts that mapping in the config directory, which the plugin must not write to
- [ ] 8.3 CHANGE — add to `SPEC.md`: Overview → stack list (audience: future changes choosing a parser) — `toml`, which is missing although the configuration format the same document specifies is TOML
- [ ] 8.4 CHANGE — add to `SPEC.md`: Architecture → module map (audience: same) — one row each for `config` and `state`, the two modules this change adds, so the map stays the index it claims to be. In the same pass, re-check the "untestable residue is two thin wrappers and `main`" sentence against the new `config::env_lookup` binding and either amend the enumeration or record that no change is needed because 2.5 gives that binding its own assertions
- [ ] 8.5 CHANGE — add to `SPEC.md`: Degraded states table (audience: `degraded-states`, which is planned from this table) — two rows: a `config.toml` that is malformed, unreadable, or carries a key of the wrong type degrades to the documented defaults for the affected keys, with the ignored keys named in `Config::problems`; an `agent-names.toml` that is unusable degrades to an empty mapping, so attribution falls back to name equality and nothing on disk is lost. Without these rows the two conditions this change can produce are the two `degraded-states` is least likely to render
- [ ] 8.6 CHANGE — rewrite in `SPEC.md`: Herdr integration → Attributing an agent, tier 1 (audience: `agent-attribution` and `agent-launch`, both planned from this paragraph) — it says only that "longer change names are truncated and the mapping is recorded", and asserts change names "are already kebab-case". Replace with the actual derivation (lowercased, illegal characters replaced, runs collapsed and ends trimmed, `c-` prefixed when the first character is not a letter, truncated to 27 characters plus a four-digit hash suffix past 32) and the actual recording rule: a mapping is recorded whenever the derived name differs from the change name, not only on truncation — `2fa-support` becomes `c-2fa-support` at 13 characters and is unattributable without it. Note in the same edit that Launch flow's `herdr agent start <change>` and `herdr agent prompt <change>` lines now mean the *derived* name, and state explicitly that correcting those two lines is `agent-launch`'s, so the obligation is recorded rather than lost
- [ ] 8.7 CHANGE — rewrite in `openspec/IMPLEMENTATION-ORDER.md`: Phase 1 table, the `plugin-config` row (audience: every later change planned from the roadmap, `repo-resolution` first) — it repeats "the directory reported by `herdr plugin config-dir`", the claim design.md → Context proves impossible for a plugin process. Replace with the injected `HERDR_PLUGIN_CONFIG_DIR` and its fallback, and say the mapping file lives under `HERDR_PLUGIN_STATE_DIR` rather than beside `config.toml`. Confirm the dependency graph needs no edge change — `subprocess-seam` is correctly absent as a dependency of `plugin-config`. This is corrected now rather than at archive time because `repo-resolution` is planned from it
- [ ] 8.8 CHANGE — add to `README.md`: Configuration (audience: users) — one sentence that the same directory is supplied to the plugin as `HERDR_PLUGIN_CONFIG_DIR`, and which path is used when the binary runs outside a Herdr-started process. `herdr plugin config-dir herdr-openspec` stays as the way a user finds the directory; it is correct and is not what the plugin calls
- [ ] 8.9 CHANGE — rewrite in `AGENTS.md`: Architecture rules, both bullets, in place (audience: every future session). The spawn bullet gains the corollary that makes it survivable: Herdr injects `HERDR_PLUGIN_CONFIG_DIR`, `HERDR_PLUGIN_STATE_DIR`, `HERDR_PLUGIN_ROOT`, and the workspace, tab, and pane ids into every plugin process it starts, so plugin context is read from the environment, never from `herdr plugin ...`. The never-write bullet currently reads as "the plugin writes nothing", which is no longer true: extend it to say the plugin's own writes — the agent-name mapping — go under `HERDR_PLUGIN_STATE_DIR` and nowhere else. Rewrite both bullets; do not append a third one beside them
- [ ] 8.10 CHANGE — add to `AGENTS.md`: Conventions (audience: same) — one line: environment-dependent code takes an injected lookup and confines `std::env::var` to a single binding, because `std::env::set_var` is `unsafe` in edition 2024 and races parallel tests
- [ ] 8.11 CHANGE — rewrite in `AGENTS.md`: Current repo state (audience: a fresh session's first paragraph) — it still says only `repo-foundation` has landed; `ci-pipeline` has since, and after this change the crate reads `config.toml` and writes under `HERDR_PLUGIN_STATE_DIR`. Bring the first sentence current and add the one clause about what the crate now does. This is a rewrite, not an addition
- [ ] 8.12 VERIFY: Net size — this group adds roughly a dozen lines across five documents and rewrites five entries in place. Confirm no entry it touched now says something false, in particular that no document still claims the crate has no third-party dependencies, that the config directory comes from a subprocess, that the plugin writes nothing, or that agent names are only ever truncated

## 9. Lint & Verify
<!-- kind: operational -->

- [ ] 9.1 CHECK: Inspect the intended verification commands and affected tiers — the unit tier (`cargo test --all-features`, `src/config.rs` and `src/state.rs`), the command checks in groups 1 and 6, the existing `tests/ci_workflow.rs` guard the `ci-workflow` delta leaves untouched, and the live check in 6.1. Coverage is affected: this change roughly triples the crate's line count
- [ ] 9.2 VERIFY: `cargo fmt --all -- --check` — clean
- [ ] 9.3 VERIFY: `cargo clippy --all-targets --all-features -- -D warnings` — 0 errors. Probe with `cargo clippy --version`, not `command -v cargo-clippy`: rustup installs that shim unconditionally, so the shim resolves even when the component is absent
- [ ] 9.4 VERIFY: `cargo test --all-features` — green, including the unchanged `ci_workflow` guard. Rust has no separate type-check step; `cargo test` and `cargo clippy` cover it
- [ ] 9.5 VERIFY: `cargo llvm-cov --fail-under-lines 80` — at or above the floor. If it falls short, add tests; never lower the threshold and never add an exclusion flag
- [ ] 9.6 VERIFY: `make check` — the single composite gate, exit 0. If it fails, name the failing sub-command rather than reporting a summary
- [ ] 9.7 VERIFY: `openspec validate plugin-config --strict` reports the change valid, with nvm ahead on `PATH` — `export PATH="$HOME/.nvm/versions/node/v24.20.0/bin:$PATH"` — since `openspec` is not on the `PATH` a non-login shell inherits here
